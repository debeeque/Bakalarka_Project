# Бакалаврская работа — Portable Network Analyzer

> Этот файл читается автоматически в начале **каждого** чата в этом проекте.
> Если что-то в проекте изменилось — обнови его, а не полагайся на память чата.

## Как со мной разговаривать

- **Только по-русски.** Не переключайся на чешский/английский без явной просьбы
  (в тексте работы — чешский, это другое).
- Не писать текст «как ИИ»: без списков с буллетами внутри глав работы, без
  воды и канцелярита. Академический чешский, сплошной текст с абзацами.
- **Нельзя ссылаться на ИИ как на источник.** Если структура кода или текста
  откуда-то взята — нужен реальный источник в библиографии.
- В коде на устройстве — никаких комментариев на русском, вообще минимум
  комментариев.

## Что за работа

| | |
|---|---|
| Тема (CZ) | Přenosné zařízení pro testování a monitorování sítí |
| Тема (EN) | Portable Device for Testing and Monitoring Networks |
| Автор | Mikhail Mukanov (login **MUK0015**) |
| Научрук | Ing. Pavel Nevlud, кабинет EA238, лаборатория EB215 |
| ВУЗ | VŠB-TUO, FEI |
| Год сдачи | **2027** — решено 27.07.2026, тогда же госы. Времени много, качество важнее скорости |
| Задание | `ThesisSpecification_MUK0015_vsboee250462E0.pdf` |
| Репозиторий | https://github.com/debeeque/Bakalarka_Project |

## Железо

- **Raspberry Pi 3 Model B+** — 4× USB, 1× встроенный Ethernet
- 2× USB/LAN адаптера в два USB-порта (выданы научруком) → `eth1`, `eth2`
- **5inch HDMI LCD V2** сенсорный дисплей (HDMI + питание с пинов), 800×480.
  Контроллер тача — **ADS7846**, то есть панель **резистивная** (проверено
  `xinput list` 03.08.2026). Отсюда её поведение: одно касание, нажим, и
  точность хуже всего по краям экрана — это физика панели, а не настройка.
  Калибровка со временем уходит.
- Wi-Fi модуль на самой малине (используется для Wi-Fi сканов через Nmap)
- Батарейного модуля и корпуса **пока нет** — научрук просил добавить

## Софт на устройстве

Каталог на малине: `~/diploma_project/` (пользователь `muk0015`)

| Файл | Что делает |
|---|---|
| `setup_network.sh` | bash: создаёт netns `analyzer_monitor` / `analyzer_sender`, перекидывает туда `eth1`/`eth2`, назначает IP, поднимает dnsmasq с `--enable-ra` |
| `gui_app.py` | Tkinter GUI «Portable Network Analyzer», 800×480, вся логика управления |
| `arp_scan.py` | ARP-скан через Scapy |
| `ra_audit.py` | анализатор Router Advertisement на Scapy (добавлен 03.08.2026) |
| `neigh_scan.py` | перечисление соседей IPv6, EUI-64, поиск цели (добавлен 03.08.2026) |
| `capture.pcap` | пример захвата |

Версии на устройстве (сверено 03.08.2026): Python 3.13.5, Scapy 2.6.1
(`python3-scapy` из apt), dnsmasq 2.91, tcpdump 4.99.5. `sudo` без пароля.
Графика — X11 + LXDE, `Xorg :0`, авторизация `/tmp/serverauth.*`; для снимка
экрана есть `scrot`. `ndisc6`/`rdisc6` не установлены и не нужны — RS шлём
Scapy'ем. Git на устройстве нет, автозапуска GUI нет, запускается руками.

**Ключевая идея архитектуры:** логическая изоляция двух тестовых портов через
**Linux Network Namespaces (netns)** — это главный технический вклад работы.

Адресация:
- `analyzer_monitor` → `eth1` → `10.0.1.10/24`, цель `10.0.1.20`
- `analyzer_sender` → `eth2` → `10.0.2.10/24`, цель `10.0.2.20`

Функционал GUI (что реально работает):
- пассивный сниффинг + счётчик трафика в реальном времени (TCP / UDP / ICMP, детект IPv6)
- ARP-скан
- измерение пропускной способности через **iperf3** (v4 и v6) — важно: `iperf3`, не `iperf`
- ping v4 / v6
- **Nmap**: LAN-скан и Wi-Fi-скан (добавлено позже, по своей инициативе)
- **аудит IPv6: разбор Router Advertisement** (секция `IPv6 Audit`, кнопка
  `RA SCAN`, выбор порта Monitor eth1 / Sender eth2) — добавлено 03.08.2026
- виртуальная экранная клавиатура + **нумпад** для ввода IP пальцем (без физической клавы)

## Подключение и отладка

- SSH: алиас **`ssh malina`** настроен на обоих ноутбуках (ключи разные, у каждого
  свой; пароль больше не нужен).
- **MAC малины: `B8:27:EB:9B:1B:43`** (Wi-Fi, префикс `b8:27:eb` = Raspberry Pi
  Foundation). Если адрес неизвестен — искать так:
  `sudo nmap -sn <подсеть>/24` и брать хост с этим MAC.
- Отпечаток хоста: `SHA256:n+ZdFPdGeFJBoQdTLEpTm9WrFJzhbiKJAJTbZgOTip4` —
  не менялся с марта 2026.
- **mDNS (`raspberrypi.local`) в домашней сети не работает — виноват роутер.**
  Разобрано 02.08.2026. На Huawei `avahi` + `nss-mdns` установлены, демон
  запущен, строка в `/etc/nsswitch.conf` правильная
  (`mdns_minimal [NOTFOUND=return]` перед `resolve`/`dns`). На малине
  `avahi-daemon` тоже активен. Но `avahi-browse -art` не видит в сети вообще
  ничего — домашний Huawei-роутер режет multicast между Wi-Fi-клиентами.
  Acer резолвит имя не через mDNS, а через DNS роутера, поэтому у него работает.
  **Со стороны ноутбуков чинить нечего.** В `~/.ssh/config` на Huawei прописан
  IP. В лаборатории сеть другая — там стоит проверить `getent hosts
  raspberrypi.local`, и если ответит, заменить IP на имя.
- Wi-Fi у малины нет в лабе → сейчас поднимается хотспот с iPhone, к нему
  цепляются и малина, и ноут. Была идея настроить eduroam на малине.
- **Huawei** — **Arch Linux** (раньше была Windows, в июне 2026 переустановка ОС),
  путь `~/Bakalarka`. На **Acer** по-прежнему Windows 11 и
  `C:\Projects\Bakalarka` — проверено 03.08.2026, работа шла именно оттуда.
  Раньше здесь было записано, что папки на `C:` больше нет; это относилось к
  Huawei и вводило в заблуждение.

## Текстовая часть (LaTeX)

- Шаблон FEI VŠB: `diploma.cls` + `diplomalst.sty`, движок — **latexmk + biber**
  (на Windows нужен Perl для latexmk; сейчас Linux — проще)
- Главный файл `BachelorThesis.tex`, главы в `Chapters/`, картинки в `Figures/`,
  коды приложений в `SourceCodes/`
- Порядок глав уже исправлен по замечанию научрука: теория идёт **перед** практикой
- Библиография: `biblatex`, style `iso-numeric` (= норма ČSN ISO 690).
  **С 03.08.2026 источники в `literature.bib`**, `\addbibresource` переключён
  на него. Шаблонный `biblatex-examples.bib` больше не подключён — его мусорные
  ключи (`cms`, `ctan`, `coffee` и прочее) остались только в
  `Chapters/TechnicalDetails.tex`, который в сборку не входит. Если этот файл
  когда-нибудь подключать — сперва вычистить из него все `\cite`.

Формальные требования факультета — в `_context/02_fei_requirements.md`.
Самое жёсткое оттуда:

- **минимум 25 страниц** от Úvod до Závěr — это регламент, не пожелание научрука
- сдача только через IS EDISON, формат **PDF/A**, **лимит 20 МБ**,
  имя файла `2027_MUK0015_BP.pdf`
- в `Literatura` только те источники, на которые есть ссылки в тексте
- в `Závěr` обязателен **отдельно обозначенный собственный вклад** + перспективы
  развития темы
- подписи под иллюстрациями, над таблицами; оформление даёт класс `diploma`,
  руками ничего не править

Реальные источники, на которые опирается работа:

1. NASTASE, R. A. *IPv6 Protocol for Beginners*. Blurb, 2024. ISBN 979-8210198082.
   → раздел автоконфигурации в теории и в практике.
2. TUIFAIGA, A. A.; RAM, P. A.; KOLAHI, S. S. *Performance Comparison of IPv6 in
   802.11ac WLAN in Windows and Linux Environment*. TENCON 2021, s. 799–804.
   doi:10.1109/tencon54134.2021.9707449 → сравнение пропускной способности.
3. GÁL, Z.; KOCSIS, G.; TAJTI, T.; TORNAI, R. *Performance evaluation of massively
   parallel and high speed connectionless vs. connection oriented communication
   sessions*. Advances in Engineering Software, 2021, 157-158, 103010.
   doi:10.1016/j.advengsoft.2021.103010 → методика измерений iperf3.
4. CARTER, T. *Network Security with Python and Scapy*. Kindle Edition, 2024.
   → Scapy, Nmap, автоматизация аудита.

RFC, добавлены 03.08.2026 (все проверены на rfc-editor.org, DOI указаны в
`literature.bib`):

5. RFC 8200 — *IPv6 Specification*, Deering, Hinden, 07/2017.
6. RFC 4291 — *IP Version 6 Addressing Architecture*, Hinden, Deering, 02/2006
   → структура адреса, EUI-64.
7. RFC 4193 — *Unique Local IPv6 Unicast Addresses*, Hinden, Haberman, 10/2005
   → обоснование `fd00::/8` для изолированного стенда.
8. RFC 4861 — *Neighbor Discovery for IPv6*, Narten и др., 09/2007 → RS/RA, DAD.
9. RFC 4862 — *IPv6 Stateless Address Autoconfiguration*, Thomson и др., 09/2007
   → SLAAC, флаги.
10. RFC 8415 — *DHCPv6*, Mrugalski и др., 11/2018 → стателфул-альтернатива.

Обзор существующих решений, добавлено 03.08.2026 (метаданные сверены через
Crossref API, DOI подтверждены):

11. COŞAR, M.; KIRAN, H. E. *Measurement of Raspberry Pi performance in network
    traffic analysis*. SIU 2018, s. 1–4. doi:10.1109/siu.2018.8404395
12. TRIPATHI, S.; KUMAR, R. *Raspberry Pi as an Intrusion Detection System, a
    Honeypot and a Packet Analyzer*. CTEMS 2018, s. 80–85.
    doi:10.1109/ctems.2018.8769135
13. HARIAWAN, F. R.; SUNARINGTYAS, S. U. *Design an Intrusion Detection System,
    Multiple Honeypot and Packet Analyzer Using Raspberry Pi 4 for Home Network*.
    QIR 2021, s. 43–48. doi:10.1109/qir54354.2021.9716189

14. RASPBERRY PI LTD. *Raspberry Pi 3 Model B+ Product Brief*, 2025.
    → **важно:** гигабитный Ethernet висит на USB 2.0, официальный потолок
    **300 Мбит/с**. Отсюда 276 и 274 Мбит/с — это предел платформы, а не сети.
    Внесено и в практику, и в Závěr как честное ограничение прибора.

> Проверено скриптом: все 14 ключей `literature.bib` процитированы в тексте,
> висячих записей нет. Требование факультета «в Literatura только то, на что
> есть ссылки» выполнено.

> На защите комиссия может спросить по **каждому** источнику, где именно он
> использован в тексте. Мусорные примеры из шаблона (`coffee.bib`,
> «I love you Coffee») должны быть вычищены.

## Что сказал научрук (последнее, самое важное)

> - расширить прототип **аккумуляторным модулем + корпусом**, куда всё смонтировано
> - текстовая часть пока короткая: должно быть **~25 страниц** от Úvod до Závěr —
>   10 страниц теории + 15 страниц практики
> - решить, когда сдавать госы: успеваешь в этом году — сдавай в этом,
>   не хватает предметов — лучше сдать в следующем и доработать работу

Раньше он же требовал: обязательно раскрыть **IPv6** в практической части;
практика должна содержать *Návrh přenosného zařízení*, *Realizace generátoru a
analyzátoru provozu*, *Zhodnocení dosažených výsledků*.

## Нерешённое / слабые места

- ~~Автономность~~ — **закрыто 02.08.2026**, эксперимент дома, протокол в
  `_context/01_device_dumps.md`. Целевым ПК выступал Huawei. Оказалось, что
  ручной `dhclient` не нужен вообще: как только устройство раздаёт адреса,
  NetworkManager на цели сам создаёт профиль и берёт и IPv4, и IPv6. Старый
  симптом был следствием обстоятельств лаборатории, а не свойством системы.

  Подтвердилось и главное про IPv6. При стателфул `dhcp-range` dnsmasq снимал
  **два** флага, а не один: M-флаг гнал цель к DHCPv6-серверу, и вдобавок у
  префикса был снят A-флаг, запрещая автоконфигурацию. Лечится одним словом
  `slaac` в `dhcp-range` — уже внесено в `setup_network.sh`. После правки ядро
  цели само построило `fd00:1::2e0:4cff:fe68:216` с меткой `proto kernel_ra`,
  то есть без единого процесса в userspace; на контрольном sender-порту со
  снятым A-флагом такого адреса не появилось.

  **Формулировка для текста уточнена:** разница не в ручном труде, а в
  необходимости процесса на цели. По IPv4 без DHCP-клиента адреса не будет
  никогда, по IPv6 ядро выдаёт link-local сразу по поднятию линка и глобальный
  адрес из префикса при поднятом A-флаге. Так утверждение не рассыплется на
  защите.
- ~~Nmap на `10.0.1.20`~~ — **разгадано 02.08.2026**, это был баг в `gui_app.py`.
  Режим LAN жёстко запускал сканер в `analyzer_sender`, а `10.0.1.20` находится
  за `analyzer_monitor`; namespace изолированы намеренно, маршрута между ними
  нет, и Nmap честно отвечал `failed to determine route`. Из правильного netns
  скан отрабатывает. Исправлено методом `netns_for()` — выбор namespace по
  подсети цели, применён в Nmap, ping и iperf3.

  **В тексте это было подано неверно и исправлено 03.08.2026.** Старая редакция
  `SampleChapter1.tex` выдавала «цели не найдены» за доказательство изоляции,
  то есть баг GUI объявлялся достижением. Теперь написано честно: Nmap отвечал
  `failed to determine route`, маршрута между netns нет намеренно (это и есть
  доказательство изоляции), а недоступность части адресов была дефектом
  интерфейса и устранена `netns_for()`.
- **Результаты эксперимента 02.08 внесены в текст 03.08.2026.** В
  `SampleChapter1.tex` — подраздел *Adresní autokonfigurace protokolu IPv6*
  (флаги M/O/A, таблица `tab:ra_flags`, ключевое слово `slaac`, `proto
  kernel_ra`, EUI-64) и подраздел *Ověření automatické konfigurace adres*
  (таблица `tab:adresy` с контрольным портом). В `Conclusion.tex` — секция
  *Vlastní přínos autora* (требование факультета) с тремя пунктами вклада.
  Источник `nastase2024` привязан к разбору автоконфигурации — первая реальная
  привязка источника к месту в тексте.
- ~~Кабель `eth1 ↔ eth2`~~ — **воткнут 03.08.2026**, анализатор RA проверен на
  железе с обоих портов. Скриншот `Figures/gui_ra_scan.png` есть, в текст ещё
  **не вставлен** — нужен подраздел в *Realizace* и результаты в *Zhodnocení*.
- ~~Второй запуск `setup_network.sh` ломал стенд~~ — **найдено и исправлено
  03.08.2026.** При `ip netns delete` физические интерфейсы возвращаются в
  дефолтный namespace асинхронно, а скрипт сразу пытался их перекинуть: eth1
  успевал, eth2 нет и оставался снаружи с пустым `analyzer_sender`. На свежей
  загрузке не проявлялось, поэтому и не всплывало раньше. Добавлено ожидание
  появления интерфейса, три запуска подряд проходят чисто.
- ~~В `gui_app.py` адреса цели зашиты жёстко~~ — **закрыто 03.08.2026.** Цели
  находятся кнопкой `NEIGHBORS`, старые константы остались только как запасной
  вариант в словаре `PORTS`, и в лог пишется, какой источник использован.
- **Точность тача по краям.** Панель резистивная (ADS7846), калибратора на
  устройстве нет. Реальное лечение — `xinput_calibrator` (пакет
  `xinput-calibrator`) с сохранением матрицы в конфиг X. Сделать это удалённо
  нельзя: нужно физически тыкать в кресты на экране. Пока не сделано.
  Промежуточно 03.08.2026 GUI переведён в полноэкранный режим и мелкие
  элементы увеличены (см. ниже), чего для самого прибора уже хватает.
- **Скриншоты в `Figures/` сняты в оконном режиме**, с панелью Raspberry Pi и
  заголовком окна. После перехода в полноэкранный режим их стоит переснять —
  прибор будет выглядеть прибором, а не программой на рабочем столе.
- **Фикс потокобезопасности GUI, 03.08.2026.** Все кнопки писали в виджеты
  Tkinter прямо из рабочих потоков. Всплыло на скриншоте: лог рисовался рваными
  кадрами и текст задваивался на экране, хотя при программном чтении лог был
  чистым. Tk из чужого потока трогать нельзя — в худшем случае это не
  косметика, а падение. Добавлен `ui()`, переносящий обращения к виджетам в
  главный поток через `root.after(0, ...)`.
- Нет аккумулятора и корпуса.
- **PDF не влезает в лимит EDISON.** Мартовская сборка — 22,8 МБ при потолке
  20 МБ. Причина: скриншоты GUI по 6–7 МБ (`Figures/` весит 47 МБ целиком),
  хотя экран устройства всего 800×480. Надо пережать. Это же было причиной,
  почему работа не уходила научруку по университетской почте.
- ~~Пять скриншотов не вставлены в текст~~ — **вставлены 03.08.2026.** `gui.JPG` и
  `gui_setup_net.JPG` в *Realizace*, `gui_pingv4/v6.JPG` и `gui_arp_scan.JPG` в
  *Zhodnocení*. Все `\ref` разрешаются, проверено пробной сборкой.
- Текст «жидкий» по наполнению. Замеры на 03.08.2026 (вечер):

  | Глава | Слов | Было 30.03 |
  |---|---|---|
  | `Introduction.tex` — Úvod | 403 | 236 |
  | `SampleChapter2.tex` — Teoretická východiska a analýza problematiky | 2 656 | 384 |
  | `SampleChapter1.tex` — Návrh a realizace, zhodnocení výsledků | 2 344 | 796 |
  | `Conclusion.tex` — Závěr | ~600 | 259 |
  | **Итого** | **~6 000** | ~1 675 |

  25 страниц — это примерно 6 000–7 000 слов, то есть **объём набран**.
  Теория закрыта целиком: 2.1 протоколы (заголовки, фрагментация, PMTUD,
  влияние на измерения), 2.2 адресация и автоконфигурация IPv6, 2.3 мониторинг
  и аудит (libpcap/BPF, TCP против UDP, ступени Nmap, правовой аспект),
  2.4 netns (что изолируется, veth, сравнение с альтернативами),
  2.5 *analýza problematiky*. Úvod переписан — там была ошибка про «пять глав»,
  хотя глав четыре.

  **Дальше проверять не количество слов, а реальные страницы** от Úvod до
  Závěr в собранном PDF: `latexmk -pdf` и посмотреть нумерацию.
- `Chapters/TechnicalDetails.tex` — остаток шаблона, в `BachelorThesis.tex` не
  подключён. Либо наполнить, либо удалить.
- `Prilohy` (`Chapters/Appendix1.tex`, `Appendix2.tex`) не наполнены; коды лежат
  отдельно в `SourceCodes/`.
- Фото схемы подключения научрук просил переделать (было «неупорядоченно»).

## План на лето 2026

Приоритет — **развитие функционала устройства**, текст пишем параллельно.

1. ~~Довести автономность~~ — **сделано 02.08.2026.**
2. Аккумуляторный модуль + корпус.
3. ~~Разобраться с Nmap на `10.0.1.20`~~ — **сделано 02.08.2026**, был баг в GUI.
4. ~~Аудит IPv6~~ — **обе части сделаны 03.08.2026**: анализатор RA и
   перечисление соседей. См. ниже отдельный блок. Осталось описать в тексте.
5. ~~Наращивать текст до 25 страниц~~ — **объём набран 03.08.2026**, ~6 000 слов.
6. ~~Вычистить библиографию~~ — **сделано 03.08.2026**, `literature.bib`, 14 источников.
7. Наполнить приложения реальными кодами.
8. Пережать `Figures/` — в самом конце, будут ещё новые скриншоты.

## Следующая функция: аудит IPv6 (решено 03.08.2026)

Выбрано из четырёх вариантов (были ещё диагностика порта через ethtool/LLDP,
углубление измерений UDP + Path MTU, железо). Причина выбора: продолжает уже
разобранную линию по RA-флагам, **демонстрируется без лабораторной
инфраструктуры** (один порт раздаёт RA, второй слушает) и попутно закрывает
зашитые адреса.

### Часть 1 — анализатор Router Advertisement: **сделана 03.08.2026**

Новый файл `ra_audit.py` — отдельный скрипт, а не код внутри GUI. Причина
техническая: `ip netns exec` меняет namespace *процесса*, а GUI живёт в
дефолтном netns, поэтому слушатель обязан быть подпроцессом. Тот же паттерн,
что у `arp_scan.py`. Побочно: импорт Scapy не тормозит GUI, и получается
самостоятельный файл для приложений работы.

Вызов: `sudo ip netns exec <ns> python3 ra_audit.py <iface> [-t сек]
[--own MAC,MAC] [--passive]`.

Поднимает `AsyncSniffer` на ICMPv6, шлёт Router Solicitation на `ff02::2`
(сырым L2 через Scapy), собирает RA и разбирает `chlim`, флаги M/O, preference,
Router Lifetime, Reachable/Retrans, а из опций — Prefix Information (префикс,
флаги L/A/R, времена жизни), MTU, RDNSS, DNSSL, Source Link-Layer Address.

Проверки аудита, каждая со ссылкой на RFC в самом тексте сообщения: чужой
маршрутизатор; hop limit ≠ 255 (RFC 4861); источник не link-local; Router
Lifetime = 0; MAC кадра ≠ MAC в опции SrcLLAddr; снятый A-флаг; длина префикса
≠ 64 при A=1 (RFC 4862); preferred больше valid; MTU < 1280; M вместе с A;
отсутствие опции префикса вообще.

**Происхождение RA делится на три случая, а не на два** — это выяснилось при
отладке и оказалось принципиальным:

| Метка | Что значит |
|---|---|
| `LOCAL TX` | кадр отправлен самим этим портом |
| `OWN DEVICE` | другой порт прибора, услышан по проводу |
| `FOREIGN` | чужой маршрутизатор |

Разделение понадобилось потому, что **Scapy через `AF_PACKET` ловит и исходящие
кадры**. Без этого прибор бодро «обнаруживал маршрутизатор в сегменте», глядя
на передачу собственного dnsmasq. Подтверждено `tcpdump -Q out` / `-Q in`: на
eth2 виден только исходящий RA, входящих нет.

MAC собственных портов собирает GUI (`device_macs()`) — читает
`/sys/class/net/<if>/address` внутри каждого netns и передаёт через `--own`.
Так надо, потому что sysfs namespace-зависим: из `analyzer_sender` MAC у `eth1`
не виден.

В GUI — отдельный `LabelFrame " IPv6 Audit "` между секцией Nmap и Dashboard:
переключатель Monitor eth1 / Sender eth2 и кнопка `RA SCAN`; на дашборде
появился индикатор `RA routers: N (foreign M)`. Проверено на экране устройства,
раскладка 800×480 держится, лог не задавлен.

**Стенд для проверки — veth-петля, без кабелей:** два временных netns
(`ra_test_a` / `ra_test_b`), соединённых veth-парой, dnsmasq с `--enable-ra` на
одном конце. Прогнаны все ветки, включая нарочно кривой RA (hop limit 64,
источник не link-local, префикс /48 при A=1, preferred > valid, MTU 1200,
подменённый SrcLLAddr) — сработали все ALERT'ы разом. Namespace'ы после тестов
удалены.

**Два результата, важных для текста:**

1. С ключевым словом `slaac` в `dhcp-range` → `A=1`, без него → `A=0`.
   Эксперимент 02.08, воспроизведённый самим прибором. Ровно демонстрация
   для защиты.
2. dnsmasq при `slaac` снимает **M=1 и A=1 одновременно**, и аудит честно
   пишет «mixed DHCPv6 and SLAAC». Это не ошибка прибора, а точное описание
   того, что делает dnsmasq — в тексте это стоит объяснить.

**Ограничение, выясненное на железе:** прибор **не может опросить собственный
RA на том порту, который его излучает**. RS уходит сырым L2-сокетом мимо
IP-стека, и dnsmasq в том же netns его не слышит; через сокет ядра
(`IPv6(dst="ff02::2%eth2")`) dnsmasq тоже не ответил. Значит RA обязан пройти
по линку.

**Отсюда рабочая топология стенда: один кабель напрямую `eth1 ↔ eth2`**, Huawei
в опыте не участвует. Проверено на железе 03.08.2026, оба линка 1000Mb/s full
duplex. dnsmasq на eth1 раздаёт RA, анализатор на eth2 слышит его как
`OWN DEVICE` и наоборот — симметрично. RS получает ответ сразу, ждать
периодического RA не приходится.

Полные выгрузки — в `_context/01_device_dumps.md`, скриншот GUI —
`Figures/gui_ra_scan.png` (дашборд показывает `RA routers: 1`).

### Часть 2 — перечисление соседей: **сделана 03.08.2026**

Файл `neigh_scan.py`. Шлёт echo request на `ff02::1`, собирает ответы, дополняет
их таблицей соседей ядра (`ip -6 neigh show dev <if>`). Для каждого MAC считает
идентификатор интерфейса по EUI-64, сверяет его с link-local соседа (так видно,
классический ли это SLAAC или непрозрачный идентификатор по RFC 7217) и
складывает кандидата для каждого префикса на порту. **Каждый кандидат
проверяется пингом**, поэтому в отчёт идёт подтверждённый адрес, а не гипотеза.
Последней строкой печатает `TARGET6`, которую разбирает GUI.

Порядок префиксов не случайный: первым идёт заданный на порту статически, а не
полученный из RA. Иначе целью выбирался бы адрес из префикса соседнего порта —
работать бы работало, но выглядело бы нелогично.

`arp_scan.py` переписан под аргументы (`arp_scan.py <iface> <сеть>`), печатает
`TARGET4` и отбрасывает собственный MAC. Заодно ушёл дефект того же рода, что и
баг с Nmap 02.08: ARP-скан был намертво прибит к `analyzer_sender` и
`10.0.2.0/24`, то есть на monitor-сегменте не работал вообще.

**Константы в `gui_app.py` собраны в один словарь `PORTS`** (ns, интерфейс,
сеть IPv4, запасные адреса) вместо россыпи по методам. Кнопка `NEIGHBORS`
запускает поиск в выбранном namespace, запоминает найденное в `self.found` и
подставляет в тесты; `PING`/`SPEED` берут цель через `target_for()` и пишут в
лог, откуда она взялась — `discovered` или `hardcoded fallback`. Тесты теперь
идут в namespace выбранного порта явно, а не через `netns_for()` по виду
адреса: найденная цель может лежать в префиксе соседнего порта.
`netns_for()` остался для поля Nmap, куда адрес вводится руками.

**Проверено на железе:** `PING v6` уходит на `fd00:2::2e0:4cff:fe68:264` —
найденный SLAAC-адрес, 4 из 4 пакетов, 0% потерь. Скриншоты
`Figures/gui_neigh_scan.png` и `Figures/gui_ping_discovered.png`.

**Контраст для текста.** На той же петле ARP-скан не находит никого: eth1 это
`10.0.1.10/24`, eth2 это `10.0.2.10/24`, разные подсети, DHCP-клиента нет. По
IPv4 сосед недостижим в принципе, по IPv6 тот же сосед на том же проводе найден,
адрес вычислен из MAC и проверен пингом — без единого процесса конфигурации.
Это подтверждает измерением уже написанный тезис про автоконфигурацию.

**Что это даст тексту:** новый подраздел в *Realizace* и результаты в
*Zhodnocení* со скриншотами; источники `rfc4861` и `rfc4862` начнут работать не
только в теории, но и в практике; из «Нерешённого» уходит пункт про зашитые
адреса. Ориентировочно +400–600 слов, то есть заодно добираются недостающие
две страницы.

> **Рабочий процесс проверен 03.08.2026 и работает:** Claude Code на Acer →
> `ssh malina`. Правим в `SourceCodes/`, копируем `scp`, сверяем `md5sum`
> с двух сторон. GUI можно запустить и снять с него скриншот прямо по SSH:
> `DISPLAY=:0 XAUTHORITY=/tmp/serverauth.* python3 gui_app.py` плюс `scrot -o`.
> Кнопку жать не обязательно — `xdotool` не стоит, callback вызывается напрямую
> из маленького харнесса, который импортирует `AnalyzerApp`.

## Решения

- **27.07.2026** — сдача перенесена на **2027**, госы тогда же.
  `\SubmissionYear{2027}` в `BachelorThesis.tex` уже проставлен.
  Следствие: есть целый год, приоритет — качество и функционал устройства,
  а не «лишь бы сдать».
- **03.08.2026** — следующая функция: **аудит IPv6** (анализатор RA +
  перечисление соседей). Подробности выше. Отложены, но не отброшены:
  диагностика порта (`ethtool` — скорость, дуплекс, автосогласование; LLDP/CDP —
  имя коммутатора и номер порта; обнаружение VLAN), измерения по UDP с потерями
  и джиттером, тест Path MTU, экспорт отчёта на флешку, модуль часов реального
  времени. **LLDP перед реализацией проверить** — если на коммутаторах в EB215
  он выключен, показывать будет нечего.
- **03.08.2026** — фотографии и скриншоты не пережимаем до самого конца:
  впереди новые эксперименты, значит будут новые изображения.
- **03.08.2026** — анализатор RA живёт **отдельным файлом** `ra_audit.py`, а не
  внутри `gui_app.py`. Довод: `ip netns exec` меняет namespace процесса,
  слушатель обязан быть подпроцессом. Из того же соображения любая будущая
  функция, которой нужен конкретный netns, делается отдельным скриптом, а GUI
  остаётся тонкой оболочкой, которая его запускает и печатает вывод в лог.
- **03.08.2026** — GUI-функции группируем секциями (`LabelFrame`), а не
  наращиваем сетку кнопок сверху: при 800×480 третий ряд кнопок съедает лог.
- **03.08.2026** — все константы портов живут в словаре `PORTS` в `gui_app.py`.
  Новый параметр порта добавляется туда, а не новой константой в методе.
- **03.08.2026** — скрипты печатают машиночитаемую последнюю строку
  (`TARGET6`, `TARGET4`), а GUI её разбирает регуляркой. Так скрипт остаётся
  самостоятельным инструментом и одновременно источником данных для интерфейса.
- **03.08.2026** — GUI работает **в полноэкранном режиме**
  (`root.attributes('-fullscreen', True)`), как киоск. Причина: на резистивном
  тачскрине трудно попасть по крестику окна и по панели задач, а полноэкранный
  режим их просто убирает — выход только через большую кнопку `EXIT`. Побочно
  вернулись ~60 px по высоте, и лог стал больше, чем был до добавления секции
  IPv6. Внутренний отступ `pady=8` у мелких кнопок и переключателей увеличил
  зону нажатия по высоте, не трогая ширину (по ширине ряд Nmap уже забит).
  Откатить — убрать одну строку с `-fullscreen`.
- **03.08.2026** — **системный интерфейс масштабировать не стали.** При 800×480
  `GDK_SCALE=2` удваивает вообще всё и делает рабочий стол непригодным, а
  подъём `Xft.dpi` раздул бы и наш Tk-интерфейс, который вымерен под этот экран
  впритык, — layout поехал бы. Правильный путь для точности касаний не
  масштаб, а калибровка панели.

## Рабочее место

Два компьютера, синхронизация через git (`git pull` до работы, `git push` после):

| | Acer (Windows 11) | Huawei (Linux) |
|---|---|---|
| Роль | 90% работы: текст, код, сборка | лаборатория, тесты с устройством |
| Путь | `C:\Projects\Bakalarka` | `~/Bakalarka` |

- LaTeX на Acer — **MiKTeX + Strawberry Perl** (latexmk требует Perl).
  WSL сознательно не ставим: мало места на диске, а линукс есть на малине и Huawei.
- Работе нужны пакеты `pdfx` (PDF/A), `biblatex`+`biber`, `babel-czech`,
  `listings`, `pdfpages`, `titlesec`. MiKTeX докачивает их сам,
  если в Console выставлено *install missing packages = Always*.
- Сборка: `latexmk -pdf BachelorThesis.tex`, чистка: `latexmk -c`
- До малины дотягивается только **Claude Code** (в вебе консоль в облаке).
  Алиас `ssh malina` из `~/.ssh/config`, не `raspberrypi.local` — mDNS дома не
  работает. Проверено 03.08.2026: правка, `scp`, запуск GUI и снятие скриншота
  с устройства делаются целиком из сессии.

Полная инструкция по установке — `SETUP.md`.

## Структура папки

```
CLAUDE.md                  ← этот файл, контекст проекта
SETUP.md                   ← одноразовый гайд по настройке рабочего места
BachelorThesis.tex         ← главный файл работы
Chapters/                  ← главы
Figures/                   ← изображения
SourceCodes/               ← коды устройства для приложений
diploma.cls, diplomalst.sty← шаблон FEI VŠB
biblatex-examples.bib      ← ВРЕМЕННО: мусор из шаблона, заменить на literature.bib
_context/
  00_supervisor_emails.md  ← вся переписка с Nevlud
  01_device_dumps.md       ← сырые выгрузки кода и логов с малины
  02_fei_requirements.md   ← формальные требования факультета
  03_session_log.md        ← журнал сессий: ход работы и почему сделано так
_archive/
  gemini_bakalarka_export.md  ← полный архив чата Gemini (81 ход, 550 КБ)
```

> **Осторожно:** `diploma.cls` вшивает задание через `\includepdf`, поэтому
> `ThesisSpecification_MUK0015_vsboee250462E0.pdf` обязан лежать в корне —
> без него работа не собирается. В `.gitignore` для него сделано исключение
> (общее правило `*.pdf` его раньше выкидывало из репозитория).
